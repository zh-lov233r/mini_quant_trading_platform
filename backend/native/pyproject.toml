[build-system]
requires = ["setuptools>=75", "wheel", "pybind11==3.1.0"]
build-backend = "setuptools.build_meta"

[project]
name = "quant-kernel"
version = "0.1.0"
description = "Native strategy and backtest kernel for the Quant platform"
requires-python = ">=3.12"

